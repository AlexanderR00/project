package com.example.alexa.repository;

import com.example.alexa.entity.StomatologyTranslate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StomatologyTranslateRepository extends JpaRepository<StomatologyTranslate,Long> {
}
